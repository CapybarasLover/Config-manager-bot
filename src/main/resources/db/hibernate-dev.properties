# Подключение к БД не задаётся здесь: URL/логин/пароль берутся из переменных окружения
# DB_LINK_DEV, DB_USER, DB_PASSWORD (см. HibernateSessionFactoryUtil).

# Echo all executed SQL to console
hibernate.show_sql=true
hibernate.format_sql=true
hibernate.highlight_sql=true